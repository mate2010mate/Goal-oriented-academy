quote = "To be or not to be, that is the question."

# Use the replace() function to replace the word "be" with "code"
modified_quote = quote.replace("be", "code")

# Print the result
print(modified_quote)