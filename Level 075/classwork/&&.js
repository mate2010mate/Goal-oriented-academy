const isOnline = true;
const hasMessages = true;

isOnline && console.log("customer is online");
hasMessages && console.log("new notification");
const age = 20;
(age >= 18) && console.log("you are adoult");
