# Creating a list of integers
numbers = [1, 2, 3, 4, 5]

# Using map to add 1 to each element in the list
new_numbers = list(map(lambda x: x + 1, numbers))

print(new_numbers)  # Output: [2, 3, 4, 5, 6]
