const age = 18;
  console.log(age >= 18 ? 'Can vote' : 'Too young'); // Can vote
  
  const isLoggedIn = false;
  console.log(isLoggedIn ? 'Welcome back' : 'Please log in'); // Please log in
  
  const num = 7;
  console.log(num % 2 === 0 ? 'Even' : 'Odd'); // Odd