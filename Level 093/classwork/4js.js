const name = "John";
const message = `Hello, ${name}!`;
console.log(message); 

const multiLineString = `This is
a multi-line
string.`;
console.log(multiLineString);

const calculateArea = (length, width) => `The area is ${length * width}`;
console.log(calculateArea(5, 10)); 
